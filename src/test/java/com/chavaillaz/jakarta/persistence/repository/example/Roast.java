package com.chavaillaz.jakarta.persistence.repository.example;

/**
 * How dark the beans were roasted, its constants deliberately alphabetical rather than lightest to darkest, so
 * that a test seeking on it exercises the same ordering regardless of direction.
 */
public enum Roast {
    DARK, LIGHT, MEDIUM
}